#The name of your workflow.
name:  Test, build and deploy
# Trigger the workflow on push or pull request
on: [push,pull_request]
#A workflow run is made up of one or more jobs. Jobs run in parallel by default.
jobs:
#pwd: /home/runner/work/skg-hagen/skg-hagen
  build:
    name: populate variable
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: populating .env
        # defining variables
        env:
          APP_API: ${{ secrets.API }}
        run: |
          # set up .env
          echo "populating .env"
          cat > ".env" <<EOF
          USERNAME = user
          PASSWORD = secret
          API = $APP_API
          EOF
        shell: bash
      - name: creating kystore file
        uses: finnp/create-file-action@1.0.0
        env:
          FILE_NAME: /tmp/kystore.kystore
          FILE_DATA: ''
      - name: creating ky properties file
        uses: finnp/create-file-action@1.0.0
        env:
          FILE_NAME: android/key.properties
          FILE_DATA: ''
      - name:
        # defining variables
        env:
          CM_KEYSTORE: ${{ secrets.CM_KEYSTORE }}
          CM_KEYSTORE_PASSWORD: ${{ secrets.CM_KEYSTORE_PASSWORD }}
          CM_KEY_ALIAS_PASSWORD:  ${{ secrets.CM_KEY_ALIAS_PASSWORD }}
          CM_KEY_ALIAS_USERNAME: ${{ secrets.CM_KEY_ALIAS_USERNAME }}
        run: |
          # set up key.properties
          echo "setting up key.properties"
          echo $CM_KEYSTORE | base64 --decode > /tmp/keystore.keystore
          echo "storePassword=$CM_KEYSTORE_PASSWORD\nkeyPassword=$CM_KEY_ALIAS_PASSWORD\nkeyAlias=$CM_KEY_ALIAS_USERNAME\nstoreFile=/tmp/keystore.keystore" > android/key.properties
        shell: bash